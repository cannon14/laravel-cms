'use strict';

var cccomus = angular.module('cccomus', [
		'ngResource',
        'ngSanitize',
		'ngTable',
        'ui.tree',
        'ngRoute',
]);
/*
.config(function($locationProvider) {
        // use the HTML5 History API
        $locationProvider.html5Mode({
            enabled: false,
            requireBase: false
        });
});
*/